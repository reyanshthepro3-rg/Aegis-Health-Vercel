{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node preinstall.js",
    "dev": "pnpm --dir artifacts/health-guide run dev",
    "build": "pnpm --dir artifacts/health-guide run build",
    "vercel-build": "pnpm --dir artifacts/health-guide run build",
    "typecheck": "pnpm --dir artifacts/health-guide run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
